use std::env;
use std::fs;
use std::io::{self, BufRead, Error, Result, Write};
use std::path::Path;

pub fn run_file(path: &str) -> io::Result<()> {
    // TODO(g4titanx): impl file reading logic
 
    Ok(())
}

pub fn run_prompt() -> io::Result<()> {
    // TODO(g4titanx): impl file reading logic 

    Ok(())
}